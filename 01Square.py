import matplotlib.pyplot as plt  #pyplot is written as plt(alias) so that we don't have to write it again and again
squares=[1,4,9,16,25]     #square list is created
plt.plot(squares)     #plt.plot plots the number
plt.show()  #plt.show() opens the matplot viewer and displays the plot

 #pyplot contains the functions to generate graphs and plot"""
